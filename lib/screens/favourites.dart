import 'package:flutter/material.dart';

class FavouritePlaces extends StatelessWidget {
  const FavouritePlaces({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Column(
        children: [Text('My Places')],
      ),
    );
  }
}
